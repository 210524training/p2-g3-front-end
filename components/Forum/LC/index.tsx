import React from 'react';
import { View } from 'react-native';
import { User } from '../../../@types';

export type LCProps = {
  user: User,
};

const LeftContainer: React.FC<LCProps> = (): JSX.Element => {
  return (
    <View>
      
    </View>
  );
};

export default LeftContainer;